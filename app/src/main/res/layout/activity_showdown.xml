<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/fondoshowdown"
    tools:context=".Vistas.Showdown">

    <EditText
        android:id="@+id/multilineequipo"
        android:layout_width="394dp"
        android:layout_height="540dp"
        android:layout_marginTop="16dp"
        android:ems="10"
        android:gravity="start|top"
        android:inputType="textMultiLine"
        android:textColor="@color/black"
        android:textIsSelectable="true"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.47"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/imgacceder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/multilineequipo"
        app:srcCompat="@drawable/botonp" />

    <TextView
        android:id="@+id/textView21"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/pokemonsolid"
        android:text="Acceder"
        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="@+id/imgacceder"
        app:layout_constraintEnd_toEndOf="@+id/imgacceder"
        app:layout_constraintStart_toStartOf="@+id/imgacceder"
        app:layout_constraintTop_toTopOf="@+id/imgacceder" />

</androidx.constraintlayout.widget.ConstraintLayout>